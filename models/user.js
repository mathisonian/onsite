var mongoose = require('mongoose')
  , Schema = mongoose.Schema;

// Declare this stuff here
// e.g.:
/*
var userSchema = new Schema({
    email: String,
    name: String
});

module.exports = mongoose.model('User', userSchema);
*/